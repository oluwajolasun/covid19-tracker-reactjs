import React from 'react';
import { Typography } from '@material-ui/core';

const Header = () => {
	return (
		<div className='header'>
			<Typography variant='h4'>Global COVID tracker</Typography>
		</div>
	);
};

export default Header;
